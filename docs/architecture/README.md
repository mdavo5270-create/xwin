# Architecture XWIN

Statut : **prévue**, pas encore implémentée.

Cibles :

- Application web publique (compétitions, comptes, paiements)
- API / services métier
- Jobs asynchrones (tirages, emails, webhooks)
- Couche de données séparée par environnement

Le monorepo accueillera `apps/` et `packages/` au moment du scaffolding, pas avant.
