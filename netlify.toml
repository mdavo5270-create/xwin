[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Runtime Next.js sur Netlify (App Router, middleware, API routes).
[[plugins]]
  package = "@netlify/plugin-nextjs"
