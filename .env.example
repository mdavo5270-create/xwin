# XWIN — variables d'environnement (exemples uniquement)
# Copier vers .env.local et remplir via Vercel / secrets GitHub.
# Ne jamais commiter de vraies clés.

# App
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_ENV=development

# API (placeholders)
DATABASE_URL=
# AUTH_SECRET=
# STRIPE_SECRET_KEY=   # production only, via Vercel Production env
# STRIPE_WEBHOOK_SECRET=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Observability (optionnel)
# SENTRY_DSN=
